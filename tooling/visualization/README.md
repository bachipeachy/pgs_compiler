# visualization

Protocol graph and execution trace rendering.

Produces DOT source and PNG output for:
- **Workflow DAGs** — static protocol structure from compiled artifacts
- **Execution traces** — runtime path overlay on the workflow graph

No protocol compilation or execution semantics live here. Input is compiled artifacts and trace files only.
